<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.markussabolins.tictactoe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TicTacToe">
        <activity
            android:name=".PVPGame"
            android:exported="false" />
        <activity
            android:name=".PVEGame"
            android:exported="false"
            android:label="@string/title_activity_pvegame"
            android:theme="@style/Theme.TicTacToe.NoActionBar" />
        <activity
            android:name=".PVESetup"
            android:exported="false" />
        <activity
            android:name=".PVPSetup"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>